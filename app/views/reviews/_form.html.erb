<% if @review.errors.any? %>
  <% @review.errors.full_messages.each do |error| %>
    <p><%= error %></p>
  <% end %>
<% end %>

<%= simple_form_for [booking, review] do |f| %>
  <%= f.input :content %>
  <%= f.input :score %>
  <%= f.submit %>
<% end %>
